# Project URL (Settings → API)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co

# Use either the legacy anon JWT or the new publishable key (Settings → API)
NEXT_PUBLIC_SUPABASE_ANON_KEY=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_DEFAULT_KEY=

NEXT_PUBLIC_ADMIN_PIN=admin1234

# After first schema install, run supabase/migration_targets_judge.sql for target allotment + optional judge code.
